<!DOCTYPE html>

<title>Edit Depenses</title>

{% block body %}
    <h1>Edit Depenses</h1>

    {{ include('depenses/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_depenses_index') }}">back to list</a>

    {{ include('depenses/_delete_form.html.twig') }}
{% endblock %}
