<!DOCTYPE html>

<title>Edit Iepp</title>

{% block body %}
    <h1>Edit Iepp</h1>

    {{ include('iepp/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_iepp_index') }}">back to list</a>

    {{ include('iepp/_delete_form.html.twig') }}
{% endblock %}
