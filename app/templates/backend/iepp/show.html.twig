<!DOCTYPE html>

<title>Iepp</title>

{% block body %}
    <h1>Iepp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ iepp.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ iepp.libelle }}</td>
            </tr>
            <tr>
                <th>Parent</th>
                <td>{{ iepp.parent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iepp_index') }}">back to list</a>

    <a href="{{ path('app_iepp_edit', {'id': iepp.id}) }}">edit</a>

    {{ include('iepp/_delete_form.html.twig') }}
{% endblock %}
