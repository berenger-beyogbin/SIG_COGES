<!DOCTYPE html>

<title>Privilege</title>

{% block body %}
    <h1>Privilege</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ privilege.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ privilege.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ privilege.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_privilege_index') }}">back to list</a>

    <a href="{{ path('app_privilege_edit', {'id': privilege.id}) }}">edit</a>

    {{ include('privilege/_delete_form.html.twig') }}
{% endblock %}
