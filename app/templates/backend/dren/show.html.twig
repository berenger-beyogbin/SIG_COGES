<!DOCTYPE html>

<title>Dren</title>

{% block body %}
    <h1>Dren</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dren.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ dren.libelle }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ dren.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dren_index') }}">back to list</a>

    <a href="{{ path('app_dren_edit', {'id': dren.id}) }}">edit</a>

    {{ include('dren/_delete_form.html.twig') }}
{% endblock %}
