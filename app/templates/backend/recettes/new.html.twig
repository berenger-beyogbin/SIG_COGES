<!DOCTYPE html>

<title>New Recettes</title>

{% block body %}
    <h1>Create new Recettes</h1>

    {{ include('recettes/_form.html.twig') }}

    <a href="{{ path('app_recettes_index') }}">back to list</a>
{% endblock %}
